apiVersion: apps/v1
kind: Deployment
metadata:
  name: employee-service-qa
  labels:
    app: employee-service-qa
spec:
  replicas: 1
  selector:
    matchLabels:
      app: employee-service-qa
  template:
    metadata:
      labels:
        app: employee-service-qa
    spec:
      containers:
        - name: employee-service
          image: employee-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://host.docker.internal:5432/employeeManagementQA"
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: username
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: password
---
apiVersion: v1
kind: Service
metadata:
  name: employee-service-qa
spec:
  type: NodePort
  selector:
    app: employee-service-qa
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30082